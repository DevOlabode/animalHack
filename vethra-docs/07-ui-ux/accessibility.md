# Accessibility (UI)

See root `ACCESSIBILITY.md` for full checklist.

MVP minimum:

- Semantic headings
- Labels on all inputs
- Focus visible rings
- Status not color-only (include text)
- Buttons are `<button>` not non-keyboard divs
- Alt text for pet photos
