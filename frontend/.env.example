# Local: leave unset so Vite proxies /api → backend
# Production (Vercel): set to your API origin including /api path if needed
# Example: VITE_API_URL=https://vethra-api.example.com/api
VITE_API_URL=
